<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 13+ 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="cc.aurora.bot.app.DefaultApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Xposed 模块元数据 -->
        <meta-data
            android:name="xposed_module"
            android:value="true" />
        <meta-data
            android:name="xposed_minversion"
            android:value="93" />
        <meta-data
            android:name="xposed_scope"
            android:value="com.tencent.mm" />
        <meta-data
            android:name="xposed_description"
            android:value="微信AI自动回复机器人" />

        <!-- Xposed 入口 -->
        <service
            android:name="cc.aurora.bot.hook.HookEntry"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="de.robv.android.xposed.module.IXposedModuleLoad" />
            </intent-filter>
            <meta-data
                android:name="xposed_module"
                android:value="true" />
            <meta-data
                android:name="xposed_minversion"
                android:value="93" />
            <meta-data
                android:name="xposed_scope"
                android:value="com.tencent.mm" />
            <meta-data
                android:name="xposed_description"
                android:value="微信AI自动回复机器人" />
        </service>

        <!-- 主界面 -->
        <activity
            android:name="cc.aurora.bot.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HTTP 服务器前台通知服务 -->
        <service
            android:name="cc.aurora.bot.service.notification.NotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>